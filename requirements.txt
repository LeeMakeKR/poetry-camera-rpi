# Poetry Camera 실행에 필요한 패키지
#
# 라즈베리파이 OS(Bookworm 이후)는 시스템 파이썬을 보호하므로 설치 시
# --break-system-packages 가 필요합니다.
#
#   pip3 install -r requirements.txt --break-system-packages
#
# picamera2 는 라즈베리파이 OS 에 기본 설치되어 있어 보통 건드릴 필요가 없습니다.

# 카메라
picamera2

# 시 생성 (Google Gemini)
google-generativeai

# .env 파일에서 API 키 읽기
python-dotenv

# 한글을 이미지로 렌더링해 프린터로 보내기 위해 필요
Pillow

# 셔터 버튼 입력
gpiozero

# 감열 프린터 시리얼 통신 (Adafruit_Thermal.py 가 사용)
pyserial

# WS2812 상태 LED
adafruit-circuitpython-neopixel
